import styled from "styled-components";

export const StyledContactsMenu = styled.nav`
	/* margin-left: auto; */
  margin-right: 46px;
  padding-top: 12px;

	& ul {
		list-style: none;
		padding: 0;
		margin: 0;
		display: flex;
		gap: 4.5rem;
	}
`;
