import styled from "styled-components";

export const StyledContactsMenu = styled.nav`
	/* margin-left: auto; */

	& ul {
		list-style: none;
		padding: 0;
		margin: 0;
		display: flex;
		gap: 4.5rem;
	}
`;
